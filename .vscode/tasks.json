{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "detail": "Add dotenv key",
      "label": "vaultkey",
      "type": "shell",
      "command": "$env:DOTENV_KEY = $(npx dotenv-vault@latest keys staging)"
    }
  ]
}
